# moonclaw Electron

under `ui/native`:

```
pnpm i
```

## How to dev

run following commands in parallel:

```
pnpm watch
pnpm dev
```

## How to build

```
pnpm package
```

The output will be in `ui/native/out` folder.
